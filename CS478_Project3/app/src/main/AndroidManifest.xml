<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="skar5k_proj3.cs478_project3">

    <permission android:name="edu.uic.cs478.project3"
        android:label = "uic 478 project 3 permission"
        android:description="@string/permis_descrip"
        android:protectionLevel="dangerous"/>
    <uses-permission android:name="edu.uic.cs478.project3"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">


        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>